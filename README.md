# Inverted-search
This project implements an Inverted Index data structure in C to efficiently store and retrieve words from multiple text files. It allows fast searching of words along with the files in which they appear and their frequency count.  The system uses a hash table combined with linked lists to organize and manage word indexing dynamically.
